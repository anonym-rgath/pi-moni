services:
  pi-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pi-monitor
    restart: unless-stopped
    networks:
      - traefik-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.pi-monitor.rule=Host(`pi-monitor.sau-index.de`)"
      - "traefik.http.routers.pi-monitor.entrypoints=web"
      - "traefik.http.services.pi-monitor.loadbalancer.server.port=80"
      - "traefik.docker.network=traefik-network"

networks:
  traefik-network:
    external: true
